#include <iostream>
#include <vector>
#include <algorithm>
#include <map>
using namespace std;

map<int, int> sum;
vector<int> v;
int n, s, ans = 0;

int main() {
	cin.tie(0); cout.tie(0);
	ios::sync_with_stdio(false);

	cin >> n >> s;

	v.resize(n);

	for (int i = 0; i < n; i++) {
		cin >> v[i];
	}
	
	


	return 0;
}